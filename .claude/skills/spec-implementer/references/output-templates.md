# Output Templates Reference

Шаблоны вывода для различных этапов работы skill.

## TOC
1. [Презентация задачи](#презентация-задачи)
2. [Сбор информации (Фаза 2)](#сбор-информации-фаза-2)
3. [Завершение этапа](#завершение-этапа)
4. [Сообщения об ошибках](#сообщения-об-ошибках)

---

## Презентация задачи

```
📋 SPEC IMPLEMENTER — Сессия загружена

📂 Spec: .ai/specs/{branch-name}.md
🌿 Ветка: {branch-name}

📌 ТЕКУЩИЙ ЭТАП: {N}. {stage_name} (~{estimated_lines} строк)

Цель: {stage_goal}

Задачи:
- [ ] {task_1}
- [ ] {task_2}

Файлы:
- {file_path_1} — {action}

Критерий приёмки: {acceptance_criteria}

Прогресс: {completed}/{total} этапов
```

## Сбор информации (Фаза 2)

### Запуск субагентов

```
🔍 СБОР ИНФОРМАЦИИ — Этап {N}: {stage_name}

Запускаю субагенты параллельно:
  [A] Code Mapper     — структура файлов, сигналы, слоты
  [B] Data Flow Tracer — request/response dict, образцы

Ожидаю результатов...
```

### Оценка достаточности

```
📋 ОЦЕНКА ТРАССИРОВКИ

  ✓ Целевые файлы прочитаны: {N}/{total}
  ✓ Сигналы/слоты идентифицированы
  ✓ Data model раскрыта: {"key": type, ...}
  ✓ Образец найден: {path}
  ✓ Архитектурный слой подтверждён: {слой}

Результат: достаточно / требуется Стадия 2 [{причина}]
```

### Запуск Стадии 2 (если нужна)

```
⚠️ Стадия 2 активирована

Причина: {незакрытый критерий}
Запускаю: {Pattern Verifier | Signal Graph Builder}
```

### Итог фазы (перед реализацией)

```
📊 ТРАССИРОВКА ЗАВЕРШЕНА — Этап {N}

Целевые файлы:
  ✓ {path} — [изменить]
  ✓ {path} — [создать], образец: {analog_path}

Data Flow:
  Action: "{action_name}"
    Request:  {"key": type}
    Response: {"key": type}
    Цепочка:  {Emitter} → base_signals → {Handler}

Образец: {path}
Стадия 2: не потребовалась / выполнена

Готовность к реализации: ✅
```

---

## Завершение этапа

```
✅ ЭТАП {N} ЗАВЕРШЁН

📋 Spec: .ai/specs/{branch-name}.md
🌿 Коммит: {commit_hash}

Выполненные задачи:
✓ {task_1}
✓ {task_2}

Изменённые файлы: {count}

🔜 Следующий шаг: {recommendation}
```

## Сообщения об ошибках

### Защищённая ветка
```
⚠️ Вы находитесь в защищённой ветке: {branch}

Переключитесь на feature-ветку или создайте ТЗ через spec-creation
```

### ТЗ не найдено
```
❌ ТЗ не найдено для ветки: {branch}

Ожидаемый файл: .ai/specs/{branch-name}.md
```

### Все этапы завершены
```
✅ Все этапы завершены!

Следующие шаги:
1. skill "gui-testing"
2. skill "merge-helper"
```

### Тесты не прошли
```
❌ Тесты не прошли

Результат: {X} failed, {Y} passed
```

### Превышен лимит
```
⚠️ Превышен лимит изменений: ~{N} строк (лимит: 250)

Разбейте этап на под-этапы
```
