# Move Calculations to Series - Feature Specification

> **Дата создания:** 2026-02-21
> **Дата завершения:** 2026-02-21
> **Ветка:** `feature/move-calculations-to-series`
> **Статус:** ✅ Завершён
> **Коммит:** cc3b903

---

## Workflow выполнения

Данное ТЗ должно выполняться согласно **генеральному workflow** из [CLAUDE.md](../../CLAUDE.md).

### Тип workflow: Новая фича

| Шаг | Действие            | Навык                                  | Статус     |
| --- | ------------------- | -------------------------------------- | ---------- |
| а   | Создание ТЗ + Ветка | —                                      | ✅ Завершён |
| б   | Реализация          | `spec-implementer`                     | ✅ Завершён |
| в   | Тестирование        | `gui-testing`                          | ✅ Завершён |
| г   | Мерж                | `merge-helper`                         | 🔄 В работе |

### Текущий статус и следующий шаг

**Текущий шаг:** г (Мерж) — 🔄 В работе
- Ветка: `feature/move-calculations-to-series`
- Коммиты: b2e308b, 848208a, cc3b903

**Следующий шаг:** Завершение (merge в main)

---

## Видение

Убрать раздел "calculations" из навигационного дерева (sidebar) и сделать расчёты (model-fit, model-free, model-based) доступными только изнутри выбранной серии данных через sub_sidebar, аналогично тому, как деконволюция доступна из экспериментальных данных.

**Проблема:** Пользователь может попытаться запустить расчёт без выбранной серии, что приводит к ошибке "It is necessary to select a series for calculation".

**Решение:** Сделать расчёты доступными только после выбора серии, устранив возможность путаницы.

**Ключевые требования:**
- Удалить раздел "calculation" из app_tree (sidebar.py)
- Добавить ActionButtonsBlock с кнопками расчётов в SeriesSubBar
- Существующие панели расчётов оставить без изменений
- Добавить обработку edge case с выводом в console

---

## План реализации

### Этап 1: Удаление раздела calculation из sidebar (~20 строк)
**Статус:** ✅ Завершён

**Цель:** Убрать раздел "calculation" и его дочерние элементы из навигационного дерева.

**Задачи:**
- [x] Удалить `self.calculation_root` и его дочерние элементы из `sidebar.py`
- [x] Удалить обработку кликов по элементам calculation в `on_item_clicked()`
- [x] Убрать emit сигнала `sub_side_bar_needed` для calculations

**Файлы:**
- `src/gui/main_tab/sidebar.py` (modify)

**Критерий приёмки:**
- Раздел "calculation" не отображается в дереве навигации
- Клик по другим элементам дерева работает корректно

**Тестирование:**
- UI: Открыть приложение, проверить отсутствие раздела "calculation" в дереве
- UI: Проверить навигацию по другим разделам (experiments, series)

---

### Этап 2: Добавление ActionButtonsBlock в SeriesSubBar (~80 строк)
**Статус:** ✅ Завершён

**Цель:** Добавить блок с кнопками для перехода к расчётам внутри панели серии.

**Задачи:**
- [x] Создать класс `CalculationButtonsBlock` (аналог `ActionButtonsBlock` из experiment)
- [x] Добавить 3 кнопки: "model fit", "model free", "model based"
- [x] Определить сигналы для переключения панелей
- [x] Интегрировать блок в layout `SeriesSubBar`
- [x] Подключить сигналы к `main_tab.py` для переключения через `SubSideHub`

**Файлы:**
- `src/gui/main_tab/sub_sidebar/series/series_sub_bar.py` (modify)
- `src/gui/main_tab/main_tab.py` (modify - подключение сигналов)

**Критерий приёмки:**
- В панели серии отображаются 3 кнопки расчётов
- При клике на кнопку происходит переключение на соответствующую панель
- Стилизация кнопок соответствует design system

**Тестирование:**
- UI: Выбрать серию в sidebar, проверить наличие кнопок расчётов
- UI: Кликнуть каждую кнопку, проверить переключение панели
- Edge case: Попытка расчёта без серии - проверить вывод в console

---

### Этап 3: Обработка edge cases и связки (~30 строк)
**Статус:** ✅ Завершён

**Цель:** Добавить обработку ошибок и связать контекст серии с расчётами.

**Задачи:**
- [x] Добавить проверку выбранной серии перед переключением панели
- [x] Выводить сообщение в console если серия не выбрана
- [x] Убедиться что расчёты получают данные выбранной серии

**Файлы:**
- `src/gui/main_tab/sub_sidebar/series/series_sub_bar.py` (modify)
- `src/gui/main_tab/main_tab.py` (modify)

**Критерий приёмки:**
- При отсутствии серии в console выводится понятное сообщение об ошибке
- Расчёты корректно получают данные выбранной серии
- Возврат к серии через клик в sidebar работает

**Тестирование:**
- Edge case: Открыть приложение, не выбирая серию проверить disabled-state кнопок
- UI: Выбрать серию, запустить расчёт, вернуться через sidebar
- Console: Проверить вывод сообщений об ошибках

---

## Технические детали

### Текущая архитектура

```
sidebar.py
├── calculation_root (QStandardItem)
│   ├── "model fit"
│   ├── "model free"
│   └── "model based"
│
└── on_item_clicked()
    └── emit sub_side_bar_needed(item_text)
```

### Целевая архитектура

```
sidebar.py
├── (calculation_root удалён)
└── on_item_clicked() - без обработки calculations

series_sub_bar.py
├── CalculationButtonsBlock (новый класс)
│   ├── model_fit_button
│   ├── model_free_button
│   └── model_based_button
└── Сигналы: calculation_clicked(str)
```

### Паттерн для реализации

Использовать существующий паттерн из `experiment_sub_bar.py`:

```python
# experiment_sub_bar.py:168-170
def emit_deconvolution_signal(self):
    self.deconvolution_clicked.emit(SideBarNames.DECONVOLUTION.value)
```

Аналогично для расчётов в серии:

```python
class CalculationButtonsBlock(QWidget):
    calculation_clicked = pyqtSignal(str)

    def __init__(self, parent=None):
        # ... кнопки ...

    def emit_model_fit(self):
        self.calculation_clicked.emit(SideBarNames.MODEL_FIT.value)

    def emit_model_free(self):
        self.calculation_clicked.emit(SideBarNames.MODEL_FREE.value)

    def emit_model_based(self):
        self.calculation_clicked.emit(SideBarNames.MODEL_BASED.value)
```

### Связи сигналов в main_tab.py

```python
# Добавить подключение:
self.sub_sidebar.series_sub_bar.calculation_buttons_block.calculation_clicked.connect(
    self.toggle_sub_sidebar
)
```

---

## Файлы изменений

| Файл | Действие | Описание |
| ---- | -------- | -------- |
| `src/gui/main_tab/sidebar.py` | modify | Удалить calculation_root и обработку |
| `src/gui/main_tab/sub_sidebar/series/series_sub_bar.py` | modify | Добавить CalculationButtonsBlock |
| `src/gui/main_tab/main_tab.py` | modify | Подключить сигналы переключения |

---

## Риски и зависимости

| Риск | Вероятность | Митигация |
| ---- | ----------- | --------- |
| Нарушение существующих связей | Низкая | Оставить панели расчётов без изменений |
| Несоответствие design system | Низкая | Использовать существующие стили кнопок |

---

## История изменений

| Дата       | Этап    | Коммит   | Описание                                              |
| ---------- | ------- | -------- | ----------------------------------------------------- |
| 2026-02-21 | MERGE   | —        | Feature merged to main                                |
| 2026-02-21 | 3       | cc3b903  | Добавлена валидация выбора серии перед расчётом       |
| 2026-02-21 | 2       | 848208a  | Добавлен CalculationButtonsBlock в SeriesSubBar       |
| 2026-02-21 | 1       | b2e308b  | Удалён раздел calculation из sidebar                  |
| 2026-02-21 | -       | 6a0345c  | ТЗ создано                                            |
